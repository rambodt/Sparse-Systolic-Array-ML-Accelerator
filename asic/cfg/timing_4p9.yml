# Optional timing override for implementation experiments.
# Invoke with:
#   make OBJ_DIR=build_runs/<name> INPUT_CFGS="cfg/systolic_array_cfg.yml cfg/systolic_array_src.yml cfg/timing_4p9.yml" <target>

vlsi.inputs:
  custom_sdc_files:
    - "constraints_4p9.tcl"
  custom_sdc_files_meta: prependlocal
